document.addEventListener('DOMContentLoaded', () => {
  const welcomeBox = document.getElementById('welcomeMessage');
  if (welcomeBox) {
    welcomeBox.textContent = 'Welcome to SIT Nagpur';
  }
});
